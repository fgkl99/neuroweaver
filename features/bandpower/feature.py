from __future__ import annotations
import pandas as pd

def compute(df: pd.DataFrame, meta: dict) -> pd.DataFrame:
    # Placeholder implementation: returns empty frame (same index)
    return pd.DataFrame(index=df.index)
